[project]
name = "bitrix-gateway"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "EHoTuK1", email = "mhernov13102@gmail.com" }
]
requires-python = ">=3.8"
dependencies = []

[project.scripts]
bitrix-gateway = "bitrix_gateway:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
