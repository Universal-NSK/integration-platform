[project]
name = "integration-platform"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = []

[tool.uv.workspace]
members = [
    "services/bitrix_gateway",
]

[dependency-groups]
dev = [
    "debugpy==1.8.1",
    "pyright>=1.1.411",
    "pytest>=8.3.5",
    "pytest-cov>=5.0.0",
    "ruff>=0.16.4",
]
